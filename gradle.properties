# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Enables parallel execution for decoupled projects
org.gradle.parallel=true

# Enables Gradle build cache
org.gradle.caching=true

# When enabled, the Configuration Cache allows Gradle to skip the configuration phase
org.gradle.configuration-cache=true

# Kotlin code style for this project
kotlin.code.style=official

# AndroidX migration flags
android.useAndroidX=true

# Maven Central Publishing & GPG Signing placeholders
# Configure these in local ~/.gradle/gradle.properties or CI environment secrets
signing.keyId=
signing.password=
signing.secretKeyRingFile=
ossrhUsername=
ossrhPassword=
sonatypeStagingProfileId=